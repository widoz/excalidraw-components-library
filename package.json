{
  "name": "excalidraw-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hand-drawn shadcn-inspired UI components for Excalidraw",
  "scripts": {
    "build": "tsx src/build.ts",
    "preset": "tsx src/preset.ts",
    "validate": "tsx src/validate.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run build && npm run validate && npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
